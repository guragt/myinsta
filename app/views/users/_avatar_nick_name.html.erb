<%= link_to image_tag(user.avatar.small_thumb.url), user_path(user), class: 'post-avatar' %>
<%= link_to "#{user.nickname}", user_path(user), class: 'post-nick nick-absolute' %>
<span class= "name-absolute"><%= user.name %></span>
