<%= simple_form_for (@active_relationships.build), remote: true do |f| %>
  <%= f.input :followed_id, as: :hidden, input_html: { value: user.id } %>
  <%= f.button :submit, t('.follow'), class: "btn btn-primary" %>
<% end %>
