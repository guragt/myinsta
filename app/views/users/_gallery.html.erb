<div class="content">
  <div class="gallery">
    <% @posts.each do |post| %>
      <div class="gallery-item">
        <%= link_to image_tag(post.image.small_thumb.url), post, class: "gallery-image" %>
      </div>
    <% end %>
  </div>
</div>
