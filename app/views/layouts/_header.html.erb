<header class="navbar navbar-fixed-top navbar-default">
  <div class="container">
    <%= link_to "MyInsta", root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <% if signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= t("views.header.account") %> <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to t("views.header.profile"), "#" %></li>
              <li><%= link_to t("views.header.settings"), edit_user_registration_path %></li>
              <li class="divider"></li>
              <li>
                <%= link_to t("views.header.log_out"), destroy_user_session_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to t("views.header.log_in"), new_user_session_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>